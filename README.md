# gitbranch
Git branch management: list, clean merged, show ages and ahead/behind.
```bash
python gitbranch.py list -a
python gitbranch.py clean --dry-run --base main
python gitbranch.py age -n 10
python gitbranch.py ahead --base main
```
## Zero dependencies. Python 3.6+.
